# Implementation Plan: Per-Request Options & with_options()

**Track ID:** request-options_20260320
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-20
**Status:** [x] Complete

## Overview

Add `RequestOptions` struct and `OpenAI::with_options()` for per-request customization (extra headers, query params, body fields, timeout). Cheap client cloning via Arc. All existing resource methods auto-inherit options.

## Phase 1: Core Infrastructure <!-- checkpoint:8789d75 -->

Build the RequestOptions type and wire it into the client's request pipeline.

### Tasks

- [x] Task 1.1: Create `src/request_options.rs` — `RequestOptions` struct with fields: `headers: Option<HeaderMap>`, `query: Option<Vec<(String, String)>>`, `extra_body: Option<serde_json::Value>`, `timeout: Option<Duration>`. Builder methods: `.header(k, v)`, `.headers(map)`, `.query_param(k, v)`, `.query(vec)`, `.extra_body(value)`, `.timeout(dur)`. Impl `Default`, `Clone`, `Debug`. Add `merge(&self, other: &RequestOptions) -> RequestOptions` that combines two options (other wins on conflict). <!-- sha:8789d75 -->
- [x] Task 1.2: Add `options: RequestOptions` field to `OpenAI` struct in `src/client.rs`. Initialize to `Default::default()` in constructors. Add `pub fn with_options(&self, options: RequestOptions) -> Self` that returns a new `OpenAI` with `self.http.clone()`, `self.config.clone()`, `self.options.merge(&options)`. <!-- sha:8789d75 -->
- [x] Task 1.3: Update `OpenAI::request()` in `src/client.rs` to apply `self.options` — merge headers into the request builder, append query params, apply timeout override. Update `send_with_retry()` to respect timeout from options. <!-- sha:8789d75 -->
- [x] Task 1.4: Add `default_headers(headers: HeaderMap)` and `default_query(query: Vec<(String, String)>)` builder methods to `ClientConfig` in `src/config.rs`. Store as `Option<HeaderMap>` and `Option<Vec<(String, String)>>`. Apply them as initial `RequestOptions` in `OpenAI::with_config()`. <!-- sha:8789d75 -->
- [x] Task 1.5: Handle `extra_body` merge in `OpenAI::post()` and `OpenAI::send_with_retry()` — when `self.options.extra_body` is set, serialize the body to `serde_json::Value`, deep-merge extra fields, then send the merged JSON. Only applies to JSON requests (not multipart). <!-- sha:8789d75 -->

### Verification

- [x] `cargo check` passes with new types
- [x] Existing tests still pass (no breaking changes)

## Phase 2: Tests <!-- checkpoint:0aee89a -->

Validate all option paths with mockito tests.

### Tasks

- [x] Task 2.1: Test `with_options()` sends extra headers — create client, call `with_options(RequestOptions::new().header("X-Custom", "test"))`, make a request, mockito asserts header present. <!-- sha:0aee89a -->
- [x] Task 2.2: Test `with_options()` sends query params — mockito matches `?foo=bar` on the request URL. <!-- sha:0aee89a -->
- [x] Task 2.3: Test `extra_body` merge — POST request with a typed body + extra_body field, mockito matches the merged JSON. <!-- sha:0aee89a -->
- [x] Task 2.4: Test `timeout` override — verify a short timeout causes a timeout error (use mockito delay). <!-- sha:0aee89a -->
- [x] Task 2.5: Test options merge precedence — client has default header "X-A: 1", `with_options` sets "X-A: 2", verify "X-A: 2" is sent. <!-- sha:0aee89a -->
- [x] Task 2.6: Test `default_headers` / `default_query` on `ClientConfig` — verify they're sent on every request. <!-- sha:0aee89a -->
- [x] Task 2.7: Test chained `with_options()` — `client.with_options(a).with_options(b)` merges correctly. <!-- sha:0aee89a -->

### Verification

- [x] All new tests pass
- [x] `cargo test` — zero failures

## Phase 3: Docs & Cleanup <!-- checkpoint:678ec4d -->

### Tasks

- [x] Task 3.1: Add `pub mod request_options;` to `src/lib.rs`, re-export `RequestOptions`. Add doc example showing `with_options()` usage. <!-- sha:8789d75 -->
- [x] Task 3.2: Update CLAUDE.md — add `request_options.rs` to architecture, document `with_options()` pattern, mark roadmap item done. <!-- sha:678ec4d -->
- [x] Task 3.3: Remove dead code — unused imports, stale comments. `cargo clippy -- -D warnings` clean. <!-- sha:678ec4d -->

### Verification

- [x] CLAUDE.md reflects current project state
- [x] Linter clean, tests pass

## Final Verification

- [x] All acceptance criteria from spec met
- [x] Tests pass
- [x] Linter clean
- [x] Build succeeds
- [x] Documentation up to date

---
_Generated by /plan. Tasks marked [~] in progress and [x] complete by /build._
